my_name <- "Monica Sun"
# Create a variable `name_length` that holds how many letters (including spaces)
# are in your name (use the `nchar()` function)
name_length <- nchar(my_name)
print(name_length)
puppies <- 2
# Create a variable `puppy_price`, which is how much you think a puppy costs
puppy_price <- "500"
# Create a variable `total_cost` that has the total cost of all of your puppies
total_cost <- puppies*puppy_price
puppies <- 2
# Create a variable `puppy_price`, which is how much you think a puppy costs
puppy_price <- "500"
# Create a variable `total_cost` that has the total cost of all of your puppies
total_cost <- "puppies*puppy_price"
print(total_cost)
print(puppies)
print(puppy_price)
print(total_cost)
puppies <- 2
# Create a variable `puppy_price`, which is how much you think a puppy costs
puppy_price <- 500
# Create a variable `total_cost` that has the total cost of all of your puppies
total_cost <- puppies * puppy_price
print(puppies * puppy_price)
max_puppies >- 4
print(max_puppies)
max_puppies >- 4
# Create a variable `max_puppies`, which is the number of puppies you can
# afford for $1,000
max_puppies <- 4
print(max_puppies)
fav_1 <- 6
fav_2 <- 24
# Divide each number by the square root of 201 and save the new value in the
# original variable
original_variable <- '/'(sqrt(201), fav_1)
original_variable <- '/'(sqrt(201), fav_2)
print(original_variable)
fav_1 <- 6
fav_2 <- 24
# Divide each number by the square root of 201 and save the new value in the
# original variable
original_variable <- '/'(sqrt(201), fav_1)
print(original_variable)
fav_1 <- 6
fav_2 <- 24
# Divide each number by the square root of 201 and save the new value in the
# original variable
original_variable <- (fav_1/sqrt(201))
original_variable <- (fav_2/sqrt(201))
print(original_variable)
?nchar()
nchar(lyric)
lyric <- "I like to eat apples and bananas"
# Use the `substr()` function to extract the 1st through 13th letters from the
# `lyric`, and store the result in a variable called `intro`
# Use `?substr` to see more about this function
intro <- substr(lyric,1,13)
# Use the `substr()` function to extract the 15th through the last letter of the
# `lyric`, and store the result in a variable called `fruits`
# Hint: use `nchar()` to determine how many total letters there are!
nchar(lyric)
substr()
?substr()
lyric <- "I like to eat apples and bananas"
# Use the `substr()` function to extract the 1st through 13th letters from the
# `lyric`, and store the result in a variable called `intro`
# Use `?substr` to see more about this function
intro <- substr(lyric,1,13)
# Use the `substr()` function to extract the 15th through the last letter of the
# `lyric`, and store the result in a variable called `fruits`
# Hint: use `nchar()` to determine how many total letters there are!
nchar(lyric)
fruits <- substr(lyric,15,32)
print(fruits)
?gsub()
fruits_e <- gsub("a", "ee")
fruits_e <- gsub("a", "ee", fruits)
print(fruits_e)
fruits_o <- gsub("a", "o", fruits)
print(fruits_o)
add_three <- function(x){
y <- 3+x
return(y)
print(7)
imperial_to_metric <- function(feet, inches){
total_feet <- feet * 12 + inches
meters <- total_feet * 0.0254
return(meters)
}
imperial_to_metric(3,2)
imperial_to_metric <- function(feet, inches){
total_feet <- feet * 12 + inches
meters <- total_feet * 0.0254
return(meters)
}
print(imperial_to_metric(2,3))
imperial_to_metric <- function(feet, inches){
total_feet <- feet * 12 + inches
meters <- total_feet * 0.0254
return(meters)
}
imperial_to_metric(3,3)
imperial_to_metric <- function(feet, inches){
total_feet <- feet * 12 + inches
meters <- total_feet * 0.0254
meters
}
imperial_to_metric <- function(feet, inches){}
imperial_to_metric <- function(feet, inches){
total_feet <- feet * 12 + inches
meters <- total_feet * 0.0254
meters
}
imperial_to_metric(3,3)
add_three <- function(x){
y <- 3+x
return(y)
}
add_three <- function(x){
y <- 3+x
return(y)
}
return(5)
add_three <- function(x){
y <- 3+x
return(y)
}
# Create a variable `ten` that is the result of passing 7 to your `add_three`
# function
ten <- add_three(7)
make_full_name <- function(first_name, last_name){
full_name <- paste(first_name, last_name)
full_name
}
make_full_name("Roger", "Qiu")
make_full_name <- function(first_name, last_name){
full_name <- paste(first_name, last_name)
full_name
}
make_full_name("Roger", "Qiu")
return("Roger", "Qiu")
make_full_name <- function(first_name, last_name){
full_name <- paste(first_name, last_name)
full_name
}
make_full_name("Roger", "Qiu")
return("Roger", "Qiu")
print(return)
make_full_name <- function(first_name, last_name){
full_name <- paste(first_name, last_name)
full_name
}
)
make_full_name("Roger", "Qiu")
make_full_name <- function(first_name, last_name) {
full_name <- paste(first_name, last_name)
full_name
}
make_full_name("Roger", "Qiu")
add_three <- function(x) {
y <- x+3
return(y)
}
return(5)
return
4
return(1)
return(y)
add_three <- function(x) {
y <- x+3
return(y)
}
3
add_three(4)
imperial_to_metric <- function(feet, inches) {
total_feet <- feet * 12 +inches
meters <- total_feet * 0.0254
return(meters)
}
imperial_to_metric(3,9)
imperial_to_metric(5,7)
names <- c("Monica", "Carol", "Roger")
print(names)
n <- 10:49
print(n)
?length()
length(n)
n+1
m <- seq(10,1)
print(m)
v3 <- n-m
print(v3)
first_ten <- 10:20
print(first_ten)
next_ten <- seq(21,30)
print(next_ten)
all_numbers <- first_ten + next_ten
print(all_numbers)
all_numbers <- '+'(first_ten, next_ten)
print(all_numbers)
